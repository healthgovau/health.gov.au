<<<<<<< HEAD
!function(e,t,a,i){"use strict";t.behaviors.fontAwesome={attach:function(e,t){a.FontAwesomeConfig={searchPseudoElements:!0}}},t.behaviors.healthMatchHeight={attach:function(t,a){e(".region-content-bottom .pane-related-content",t).matchHeight(),e(".region-content-bottom .pane-title",t).matchHeight(),e(".view-categories .views-row .uikit-card",t).matchHeight(),e(".view-categories .views-row .uikit-card h3",t).matchHeight(),e(".region-highlighted .uikit-card .bean-title",t).matchHeight({byRow:!1}),e(".region-highlighted .uikit-card",t).matchHeight(),e(".selector-card",t).matchHeight(),e(".page-title .col",t).matchHeight(),e(".page-node-1021 .field-name-field-related-term",t).matchHeight(),e(".front .paragraphs-item-para-block.para-container.block__count--5 .bean-block-content a",t).matchHeight(),e(".feedback-col",t).matchHeight(),e(".group-statistic .field-name-field-statistic-value-text",t).matchHeight(),e(".group-trend",t).matchHeight(),e("#block-bean-homepage-healthcare-system .group-left, #block-bean-homepage-healthcare-system .group-right",t).matchHeight(),e("#block-bean-ageing-and-aged-care .bean-image-and-text, #block-bean-homepage-portfolio-sport .bean-image-and-text",t).matchHeight(),e(".paragraphs-item-para-block.block-id__more-services-card .bean-image-and-text a",t).matchHeight(),e(".view-mode-view_selector .bean-block-content a",t).matchHeight(),e(".view-mode-solid_full_card .bean-block-content a",t).matchHeight()}},t.behaviors.healthCloseHealthAlert={attach:function(t,a){e("#close-health-alert",t).click(function(a){a.preventDefault(),e("body",t).toggleClass("health-alert-inactive")})}},t.behaviors.externalLinks={attach:function(a,i){t.health.externalLinks(),e("a[rel=external]",a).parent("li").addClass("external-link")}},t.behaviors.singularResult={attach:function(t,a){"1"===e(".view-header span",t).text()?e(".view-header span",t).after(" result"):e(".view-header span",t).after(" results")}}}(jQuery,Drupal,this,this.document);
=======
/**
 * @file
 * A JavaScript file for the theme.
 *
 * In order for this JavaScript to be loaded on pages, see the instructions in
 * the README.txt next to this file.
 */

// JavaScript should be made compatible with libraries other than jQuery by
// wrapping it with an "anonymous closure". See:
// - https://drupal.org/node/1446420
// - http://www.adequatelygood.com/2010/3/JavaScript-Module-Pattern-In-Depth
(function ($, Drupal, window, document) {

  'use strict';

  // Enable fontawesome pseudo element.
  Drupal.behaviors.fontAwesome = {
    attach: function (context, settings) {
      window.FontAwesomeConfig = {
        searchPseudoElements: true
      };
    }
  };

  Drupal.behaviors.healthMatchHeight = {
    attach: function (context, settings) {

      // Match the related topics height
      $('.region-content-bottom .pane-related-content', context).matchHeight();
      $('.region-content-bottom .pane-title', context).matchHeight();

      // Match the categories height
      $('.view-categories .views-row .au-card', context).matchHeight();
      $('.view-categories .views-row .au-card h3', context).matchHeight();

      // Home page highlighted
      $('.region-highlighted .au-card .bean-title', context).matchHeight({byRow: false});
      $('.region-highlighted .au-card', context).matchHeight();

      // Listing page selector cards.
      $('.selector-card', context).matchHeight();

      // Title section match height.
      $('.page-title .col', context).matchHeight();

      // Lifestage on topic page
      $('.page-node-1021 .field-name-field-related-term', context).matchHeight();

      // Front page task accelerator.
      $('.front .paragraphs-item-para-block.para-container.block__count--5 .bean-block-content a', context).matchHeight();

      // Front page feedback band.
      $('.feedback-col', context).matchHeight();

      // Statistics
      $('.group-statistic .field-name-field-statistic-value-text', context).matchHeight();
      $('.group-trend', context).matchHeight();

      // About us boxes
      $('#block-bean-homepage-healthcare-system .group-left, #block-bean-homepage-healthcare-system .group-right', context).matchHeight();

      // Homepage - In our portfolio
      $('#block-bean-ageing-and-aged-care .bean-image-and-text, #block-bean-homepage-portfolio-sport .bean-image-and-text', context).matchHeight();

      // Immunisation more on immunisation band
      $('.paragraphs-item-para-block.block-id__more-services-card .bean-image-and-text a', context).matchHeight();

      // Front page feedback band.
      $('.view-mode-view_selector .bean-block-content a', context).matchHeight();
    }
  };

  Drupal.behaviors.healthCloseHealthAlert = {
    attach: function (context, settings) {

      // Hide the alert when the close button is pressed.
      $('#close-health-alert', context).click(function(e){
        e.preventDefault();
        $('body', context).toggleClass('health-alert-inactive');
      });

    }
  };

  // Add classes and elements to external links.
  Drupal.behaviors.externalLinks = {
    attach: function (context, settings) {
      // Add external links.
      Drupal.health.externalLinks();
      // Add class to any li surrounding an external link.
      $('a[rel=external]', context).parent('li').addClass('external-link');
    }
  };

  // Use singular when there is only 1 result in the view list.
  Drupal.behaviors.singularResult = {
    attach: function (context, settings) {
      var total = $('.view-header span', context).text();
      if (total === '1') {
        $('.view-header span', context).after(' result');
      }
      else {
        $('.view-header span', context).after(' results');
      }
    }
  };

})(jQuery, Drupal, this, this.document);
>>>>>>> Compile css and js
